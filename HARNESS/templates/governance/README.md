# Governance Starter Files

Copy the files in this directory to the repository root, remove the `.example`
suffix, and replace every example value. Copy `ACTIVE_CHANGE.example` to
`HARNESS/ACTIVE_CHANGE`.

Do not mark Gate 2 or Gate 4 complete until the named accountable person has
actually confirmed or approved the active change.
